# Presentation Layer
Esta camada expõe os endpoints REST para o frontend, interagindo com a camada de aplicação. Controladores são responsáveis por mapear requisições HTTP para ações específicas.

Aplicando SOLID: 
- **Interface Segregation Principle (ISP)**: Controladores expõem apenas os métodos necessários, mantendo a interface coesa.
