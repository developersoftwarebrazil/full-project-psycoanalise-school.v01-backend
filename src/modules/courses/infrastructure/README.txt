# Infrastructure Layer
Esta camada é responsável pela implementação dos repositórios e interações com o banco de dados. Ela utiliza o Prisma para acessar o banco de dados.

Aplicando SOLID:
- **Dependency Inversion Principle (DIP)**: A infraestrutura depende das abstrações (interfaces) fornecidas pela camada de domínio.
